const { sendValidationEmail, loginUser, registerUser, authenticateReqToken } = require('./auth_controllers');

module.exports = {
    sendValidationEmail, loginUser, registerUser, authenticateReqToken
};